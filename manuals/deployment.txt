# TODO: update these logs to do more things now that github is integrated.
# TODO: Add template for sudo sed command to replace bot key in GruVi.service
# TODO: Allow it to have a name other than GruVi
# TODO: Make this a proper deployment script

#sudo apt update -y
#sudo apt-get install python3-venv -y
sudo yum update -y
rm -rf ~/gruviVenv
python3 -m venv ~/gruviVenv
. ~/gruviVenv/bin/activate
rm -rf ~/gruviRepo
mkdir ~/gruviRepo
cd ~/gruviRepo

pip install -r ~/gruviRepo/manuals/requirements.txt

sudo cp ~/gruviRepo/manuals/GruVi.service /etc/systemd/system/GruVi.service

sudo systemctl daemon-reload
sudo systemctl enable GruVi
sudo systemctl start GruVi

cat manuals/basicBash.rc >> ~/.bashrc
